class Solution(object):
    def searchInsert(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: int
        """
        start=0
        end= len(nums)- 1 

        while start<end:
            print(start)
            print(end)
            mid = (start+end)/2
            if target > nums[(start+int(end/2))]:
                start= start + end/2 +1
            elif target < nums[(start+int(end/2))]:
                end= end - end/2 -1 
            else:
                return end
        return start
